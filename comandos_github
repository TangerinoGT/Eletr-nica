$ git init 
(cria novo subdiretório .git)

$ git status

$ git clone https://github.com/TangerinoGT/IoT_2017
$ git add <file>
$ git diff
(To see what you’ve changed but not yet staged)

$ git fetch
$ git commit -m "Story 182: Fix benchmarks for speed"
$ git pull (copiar da origem)
$ git push (enviar para origem)


$ git log
(lists the commits made in that repository in reverse chronological order)
